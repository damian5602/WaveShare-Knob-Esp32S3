esphome:
  name: esp32u4audioside
  friendly_name: esp32u4audioside

esp32:
  board: esp32dev
  framework:
    type: arduino

# Enable logging
logger:

# Enable Home Assistant API
api:
  encryption:
    key: ""

ota:
  - platform: esphome
    password: ""

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: ""
    password: ""

captive_portal:
i2s_audio:
  - id: i2sout
    i2s_lrclk_pin: GPIO27
    i2s_bclk_pin: GPIO25

media_player:
  - platform: i2s_audio
    dac_type: external
    i2s_dout_pin: GPIO26
    mode: stereo
    id: mp
    i2s_audio_id: i2sout
    name: "PLAYER"
switch:
  - platform: gpio
    pin: GPIO32
    name: "MUTE"
